[package]
name = "pinocchio-utils-macro"
description = "Pinocchio helpers"
version = "0.1.0"

[lib]
proc-macro = true

[dependencies]
proc-macro2 = "1.0"
quote = "1.0"
regex = { version = "1" }
syn = { version = "1.0", features = ["extra-traits", "full"] }
once_cell = "1.17"

